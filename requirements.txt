# NOTE: pydantic v2 uses a Rust-backed core library (pydantic-core).
# For Render / production deployment:
# 1. Add `runtime.txt` with "python-3.13.0" (PyO3 support; Python 3.14+ requires stable ABI).
# 2. Build command: `pip install -r requirements.txt`.
# See the "Render deployment" section of README.md.

fastapi==0.115.2
uvicorn[standard]==0.31.1
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.12
httpx==0.27.2
pytest==8.3.3
PyJWT==2.9.0
cryptography==43.0.3
supabase==2.7.4
groq==0.11.0
vosk==0.3.45
websockets==14.1
gunicorn==23.0.0
celery==5.4.0
